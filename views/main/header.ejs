<header class="header">
  <div class="logo">
    <a href="/" class="logo__text"><%= title %></a>
  </div>

  <nav class="nav">
    <% navigation.map((navItem) => { %>
      <a class="nav__link <%= navItem.url === currentUrl ? 'active' : '' %>" href="<%= navItem.url %>">
        <%= navItem.name %>
      </a>
    <% }) %>
  </nav>
</header>
